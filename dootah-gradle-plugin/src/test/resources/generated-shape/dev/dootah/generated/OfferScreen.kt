package dev.dootah.generated

import ui.BundleNode
import ui.Column

/**
 * GENERATED BY DOOTAH -- DO NOT EDIT
 *
 * Stands in for the compiler's output while the generator is being built, so
 * the bundle build can be verified against the shape it will actually receive.
 *
 * Derived from:
 *
 *     @Bundlable
 *     @Composable
 *     fun OfferScreen() {
 *         val price = 999
 *         val discount = 100
 *         val finalPrice = price - discount
 *         Column {
 *             Text("Dootah OTA Works")
 *             Text("Price: Rs ${'$'}finalPrice")
 *             Button(onClick = { }) { Text("Buy") }
 *         }
 *     }
 */
internal object OfferScreen {

    const val SCREEN_ID: String = "com.example.cahier.OfferScreen"

    private const val PRICE = 999
    private const val DISCOUNT = 100

    private fun finalPrice(): Int = PRICE - DISCOUNT

    fun render(): BundleNode =
        Column {
            Text("Dootah OTA Works")
            Text("Price: Rs ${finalPrice()}")

            Button(text = "Buy", action = "buy")
        }

    fun handle(action: String): BundleNode = render()
}
